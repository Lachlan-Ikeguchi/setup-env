#!/usr/bin/env bash

LOGDIR="$HOME/update_logs/$(date +%s)"
mkdir -p "$LOGDIR"

nix profile upgrade --all 1>>"$LOGDIR/nix" 2>&1 &

julia -e "using Pkg;  Pkg.update()" 1>>"$LOGDIR/julia" 2>&1 &

rustup update 1>>"$LOGDIR/rust" 2>&1 &
